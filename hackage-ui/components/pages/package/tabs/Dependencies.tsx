import s from './Dependencies.module.css';
import { PackageProps } from '../common';

const Dependencies = (props: PackageProps) => {
  return (
    <div className={s.dependencies}>
      Dependencies
    </div>
  );
}

export default Dependencies;
